{{- if .Values.role.rules -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "fia-svc.serviceAccountName" . }}-rb
subjects:
- kind: ServiceAccount
  name: {{ include "fia-svc.serviceAccountName" . }}
roleRef:
  kind: Role
  name: {{ include "fia-svc.serviceAccountName" . }}-role
  apiGroup: rbac.authorization.k8s.io
{{- end }}