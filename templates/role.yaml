{{- if .Values.role.rules -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "fia-svc.serviceAccountName" . }}-role
{{- with .Values.role.rules }}
rules:
{{ toYaml . }}
{{- end }}
{{- end }}